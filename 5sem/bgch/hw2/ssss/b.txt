  for (i = 0, j = 0; buf[i] && s[j]; i++, j++)
          if (buf[i] != s[j])
      if (buf[i] != s[j])
  for (i = 0; s[i]; i++)  // CHECK string s
      if (s[i] == '\\')
          if ((s[i] != '^') && (s[i] != '\\'))
  //     s_new[i] = s[j];
      for (i = 0; buf[i]; i++)
          if (buf[i] == '\n')
              buf[i] = 0;
      for (i = 0; buf[i]; i++)
